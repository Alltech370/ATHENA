// Configuração do backend
// Em produção (Koyeb), o frontend e backend estão no mesmo domínio
// Então usa window.location.origin (mesmo domínio)
// Este arquivo pode ser usado para desenvolvimento local se necessário
window.API_BASE_URL = window.API_BASE_URL || '';

